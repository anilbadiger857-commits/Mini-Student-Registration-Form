# Mini-Student-Registration-Form
A student registration form that collects name, email, and phone with JavaScript validation to ensure proper data format. Upon submission, it converts the form data to JSON format and displays it on screen in a formatted code block.
